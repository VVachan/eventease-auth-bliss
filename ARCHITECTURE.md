# 🏗️ Architecture & System Design

## Complete System Architecture

```
┌──────────────────────────────────────────────────────────────────┐
│                        USER BROWSER                              │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │            React Application (Vite)                       │ │
│  │                                                           │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │          App.tsx (Root Component)               │  │ │
│  │  │  - QueryClientProvider                         │  │ │
│  │  │  - LanguageProvider                            │  │ │
│  │  │  - AuthProvider ← NEW!                         │  │ │
│  │  │  - TooltipProvider                             │  │ │
│  │  │  - BrowserRouter (React Router)                │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  │                        ↓                               │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │        Routes & ProtectedRoute ← NEW!           │  │ │
│  │  │                                                 │  │ │
│  │  │  /auth → Auth Component (no protection)       │  │ │
│  │  │  /      → ProtectedRoute → Dashboard           │  │ │
│  │  │  /*     → ProtectedRoute → Other Pages         │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  │                        ↓                               │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │     AuthContext (Global Auth State) ← NEW!      │  │ │
│  │  │                                                 │  │ │
│  │  │  • session: Supabase Session | null            │  │ │
│  │  │  • user: Supabase User | null                  │  │ │
│  │  │  • isLoading: boolean                          │  │ │
│  │  │  • signOut(): Promise<void>                    │  │ │
│  │  │                                                 │  │ │
│  │  │  → useAuth() hook for components               │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  │                        ↓                               │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │          Components                             │  │ │
│  │  │                                                 │  │ │
│  │  │  • Dashboard (events, registrations)            │  │ │
│  │  │  • LoginForm (email/password)                   │  │ │
│  │  │  • SignupForm (create account)                  │  │ │
│  │  │  • Navbar (logout, language switch)             │  │ │
│  │  │  • AppSidebar (navigation, logout)              │  │ │
│  │  │  • EventCard, VenueCard, VendorCard             │  │ │
│  │  │                                                 │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  │                        ↓                               │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │         Supabase Client (supabase-js)           │  │ │
│  │  │                                                 │  │ │
│  │  │  • Auth Module                                  │  │ │
│  │  │    - signUp(email, password)                    │  │ │
│  │  │    - signInWithPassword(email, password)        │  │ │
│  │  │    - signOut()                                  │  │ │
│  │  │    - onAuthStateChange()                        │  │ │
│  │  │                                                 │  │ │
│  │  │  • Database Module                              │  │ │
│  │  │    - from('table_name').select()                │  │ │
│  │  │    - from('table_name').insert()                │  │ │
│  │  │    - from('table_name').update()                │  │ │
│  │  │    - from('table_name').delete()                │  │ │
│  │  │                                                 │  │ │
│  │  │  Config:                                        │  │ │
│  │  │  • VITE_SUPABASE_URL                            │  │ │
│  │  │  • VITE_SUPABASE_PUBLISHABLE_KEY                │  │ │
│  │  │  • localStorage for session persistence         │  │ │
│  │  │  • Auto token refresh enabled                   │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
                               ↓
                      HTTPS API Calls
                               ↓
┌──────────────────────────────────────────────────────────────────┐
│                      SUPABASE BACKEND                            │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │         Supabase Auth Service                           │   │
│  │                                                         │   │
│  │  • User Management                                      │   │
│  │  • Email Authentication                                │   │
│  │  • Password Hashing (bcrypt)                           │   │
│  │  • Session Management                                  │   │
│  │  • JWT Token Generation                                │   │
│  │  • Email Templates                                     │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │         PostgreSQL Database                             │   │
│  │                                                         │   │
│  │  Tables (with RLS enabled):                             │   │
│  │                                                         │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │ auth.users (Supabase managed)                   │   │   │
│  │  │ - id (UUID)                                     │   │   │
│  │  │ - email                                         │   │   │
│  │  │ - password_hash                                 │   │   │
│  │  │ - created_at                                    │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  │                                                         │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │ public.users (User profiles)                    │   │   │
│  │  │ - id (Foreign Key → auth.users)                 │   │   │
│  │  │ - email                                         │   │   │
│  │  │ - full_name                                     │   │   │
│  │  │ - avatar_url                                    │   │   │
│  │  │ - created_at, updated_at                        │   │   │
│  │  │                                                 │   │   │
│  │  │ RLS Policy:                                     │   │   │
│  │  │ Users can only view/edit their own profile      │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  │                                                         │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │ public.events (Event management)                │   │   │
│  │  │ - id, user_id (creator)                         │   │   │
│  │  │ - title, description, date                      │   │   │
│  │  │ - location, capacity, image_url                 │   │   │
│  │  │ - status (draft/published)                      │   │   │
│  │  │                                                 │   │   │
│  │  │ RLS Policies:                                   │   │   │
│  │  │ • Anyone can view published events              │   │   │
│  │  │ • Creator can view/edit their own               │   │   │
│  │  │ • Only creator can create/delete                │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  │                                                         │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │ public.registrations (Event registrations)      │   │   │
│  │  │ - id, user_id, event_id                         │   │   │
│  │  │ - status, registered_at                         │   │   │
│  │  │                                                 │   │   │
│  │  │ RLS Policies:                                   │   │   │
│  │  │ Users can only view their own registrations     │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  │                                                         │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │ public.venues (Venue management)                │   │   │
│  │  │ - id, user_id (creator)                         │   │   │
│  │  │ - name, location, capacity                      │   │   │
│  │  │ - contact info                                  │   │   │
│  │  │                                                 │   │   │
│  │  │ RLS Policies:                                   │   │   │
│  │  │ • Anyone can view all venues                    │   │   │
│  │  │ • Creator can edit/delete their own             │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  │                                                         │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │ public.vendors (Vendor management)              │   │   │
│  │  │ - id, user_id (creator)                         │   │   │
│  │  │ - name, category, contact info                  │   │   │
│  │  │                                                 │   │   │
│  │  │ RLS Policies:                                   │   │   │
│  │  │ • Anyone can view all vendors                   │   │   │
│  │  │ • Creator can edit/delete their own             │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  │                                                         │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │ public.notifications (Notifications)            │   │   │
│  │  │ - id, user_id, title, message                   │   │   │
│  │  │ - type, read, created_at                        │   │   │
│  │  │                                                 │   │   │
│  │  │ RLS Policies:                                   │   │   │
│  │  │ Users can only view their own notifications     │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  │                                                         │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │         Supabase Features                               │   │
│  │                                                         │   │
│  │  • Row-Level Security (RLS) - Enforced on all tables   │   │
│  │  • Real-time subscriptions - Optional for live updates │   │
│  │  • Automatic backups                                    │   │
│  │  • Email service integration                            │   │
│  │  • Storage (if needed for files/images)                │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
                               ↑
                        Managed by Supabase
                               ↑
┌──────────────────────────────────────────────────────────────────┐
│                       VERCEL HOSTING                             │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │         Frontend Deployment                             │   │
│  │                                                         │   │
│  │  • Build: bun run build → dist/                        │   │
│  │  • CDN: Edge network worldwide                         │   │
│  │  • HTTPS: Automatic SSL/TLS                            │   │
│  │  • Auto-deploy: On every push to main                  │   │
│  │  • Preview deployments: For every PR                   │   │
│  │  • Analytics: Real User Monitoring                     │   │
│  │  • Environment Variables: Secure storage               │   │
│  │                                                         │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
                               ↑
                        GitHub Integration
                               ↑
┌──────────────────────────────────────────────────────────────────┐
│                        GITHUB REPO                               │
│                                                                  │
│  • Source code management                                       │
│  • Version control                                              │
│  • Deployment triggers                                          │
│  • Secrets management                                           │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagrams

### 1. User Sign Up Flow
```
User                Browser              Supabase
 │                    │                     │
 ├─ Click Sign Up ───→│                     │
 │                    │ signUp(email, pwd)  │
 │                    ├────────────────────→│
 │                    │                     ├─ Hash password
 │                    │                     ├─ Create user
 │                    │ ← Returns session   │
 │                    │                     │
 │  ← Redirected ─────│ ← Save session      │
 │    to dashboard    │   (localStorage)    │
```

### 2. User Login Flow
```
User                Browser              Supabase
 │                    │                     │
 ├─ Click Sign In ───→│                     │
 │                    │ signInWithPassword  │
 │                    ├────────────────────→│
 │                    │                     ├─ Verify password
 │                    │                     ├─ Generate JWT
 │                    │ ← Returns session   │
 │                    │                     │
 │  ← Redirected ─────│ ← Save session      │
 │    to dashboard    │   (localStorage)    │
```

### 3. Protected Route Flow
```
User                App                  AuthContext
 │                   │                       │
 ├─ Visit /app ─────→│                       │
 │                   ├─ Check auth status ──→│
 │                   │ ← isLoading: true     │
 │  ← Loading ────────│                       │
 │                   │                       │
 │                   │ ← isLoading: false    │
 │                   │   user: User | null   │
 │                   │                       │
 │  User found?  ────│                       │
 │  YES ──────────→ Render Dashboard        │
 │  NO ───────────→ Redirect to /auth       │
```

### 4. Database Query with RLS Flow
```
Frontend              Supabase Client        RLS Policy         Database
   │                       │                    │                  │
   ├─ Fetch events ───────→│                    │                  │
   │                       ├─ Add JWT token ───→│                  │
   │                       │                    ├─ Check auth      │
   │                       │                    ├─ Apply RLS ─────→│
   │                       │                    │                  │
   │                       │ ← Filtered data ───│ ← Return rows    │
   │ ← Parse response ─────│                    │                  │
```

---

## Technology Stack Diagram

```
                    ┌──────────────────┐
                    │   User Browser   │
                    └────────┬─────────┘
                             │
                    ┌────────▼─────────┐
                    │   React 18       │
                    │ TypeScript/Vite  │
                    └────────┬─────────┘
                             │
         ┌───────────────────┼───────────────────┐
         │                   │                   │
    ┌────▼────┐        ┌────▼────┐       ┌─────▼────┐
    │ TanStack │        │ React   │       │ Shadcn   │
    │ Query    │        │ Hook    │       │ UI       │
    │ (State)  │        │ Form    │       │ Tailwind │
    └────┬────┘        └────┬────┘       └─────┬────┘
         │                   │                   │
         └───────────────────┼───────────────────┘
                             │
                    ┌────────▼─────────┐
                    │ Supabase Client  │
                    │   (supabase-js)  │
                    │                  │
                    │ • Auth Module    │
                    │ • DB Module      │
                    │ • Session Mgmt   │
                    └────────┬─────────┘
                             │
                    ┌────────▼─────────┐
                    │     HTTPS        │
                    │   Encrypted      │
                    └────────┬─────────┘
                             │
         ┌───────────────────┼───────────────────┐
         │                   │                   │
    ┌────▼────────┐   ┌─────▼──────┐   ┌───────▼────┐
    │  Auth API   │   │  Rest API  │   │ Realtime   │
    │             │   │  (GraphQL) │   │ WebSocket  │
    │             │   │            │   │            │
    └────┬────────┘   └─────┬──────┘   └───────┬────┘
         │                   │                   │
         └───────────────────┼───────────────────┘
                             │
         ┌───────────────────┼───────────────────┐
         │                   │                   │
    ┌────▼────────┐   ┌─────▼──────┐   ┌───────▼────┐
    │   Auth      │   │ PostgreSQL │   │  Storage   │
    │  Service    │   │  Database  │   │  Service   │
    │             │   │            │   │            │
    │ • JWT Mgmt  │   │ • RLS      │   │ • Files    │
    │ • Email     │   │ • Queries  │   │ • Images   │
    │ • Password  │   │ • Triggers │   │ • CDN      │
    └─────────────┘   └────────────┘   └────────────┘
```

---

## Deployment Architecture

```
┌─────────────────────────────┐
│    GitHub Repository        │
│                             │
│  • Source Code              │
│  • package.json             │
│  • vercel.json              │
│  • .env.gitignore           │
└──────────────┬──────────────┘
               │
         (git push origin main)
               │
        ┌──────▼────────┐
        │  GitHub Hook  │
        │  Notification │
        └──────┬────────┘
               │
        ┌──────▼──────────┐
        │  Vercel Build   │
        │  System         │
        │                 │
        │ 1. Clone repo   │
        │ 2. bun install  │
        │ 3. bun build    │
        │ 4. Deploy dist/ │
        └──────┬──────────┘
               │
        ┌──────▼──────────────┐
        │  Vercel CDN         │
        │  (Edge Network)     │
        │                     │
        │ • Caching           │
        │ • Compression       │
        │ • HTTPS             │
        │ • Auto-scaling      │
        └──────┬──────────────┘
               │
        ┌──────▼──────────────┐
        │  Live URL           │
        │                     │
        │  https://app.url    │
        │  (Publicly Shared)  │
        └─────────────────────┘
                ↓
        Supabase Connection
        (Credentials in Env Vars)
```

---

## Security Architecture

```
┌─────────────────────────────────────────────┐
│           SECURITY LAYERS                   │
└─────────────────────────────────────────────┘

Layer 1: Transport Security
├─ HTTPS/TLS (Vercel enforced)
├─ Encrypted in transit
└─ Certificate management (Vercel)

Layer 2: Authentication
├─ Email/Password signup
├─ Bcrypt password hashing (Supabase)
├─ JWT tokens (HTTP-only cookies optional)
├─ Session persistence (secure)
└─ Auto token refresh

Layer 3: Authorization
├─ Row-Level Security (RLS) Policies
├─ User isolation by auth.uid()
├─ Table-level access control
├─ Policy-based filtering
└─ Database-enforced security

Layer 4: Data Protection
├─ User data encrypted at rest (Supabase)
├─ Automatic backups
├─ Point-in-time recovery
├─ Audit logs available
└─ GDPR compliance

Layer 5: API Security
├─ Rate limiting (Supabase)
├─ DDoS protection (Cloudflare)
├─ SQL injection prevention
├─ XSS protection (React)
└─ CSRF protection

Layer 6: Environment Security
├─ Secrets in environment variables
├─ No credentials in code
├─ .env files in .gitignore
├─ Vercel secrets vault
└─ Limited access controls
```

---

## User Journey & Component Hierarchy

```
App.tsx (Root)
├─ QueryClientProvider
├─ LanguageProvider
├─ TooltipProvider
└─ AuthProvider ← Manages global auth state
   └─ BrowserRouter
      ├─ Route: /auth
      │  └─ Auth Component
      │     ├─ LoginForm
      │     │  ├─ Email Input
      │     │  ├─ Password Input
      │     │  └─ Submit Button (calls supabase.auth.signInWithPassword)
      │     └─ SignupForm
      │        ├─ Full Name Input
      │        ├─ Email Input
      │        ├─ Password Input
      │        ├─ Confirm Password Input
      │        └─ Submit Button (calls supabase.auth.signUp)
      │
      └─ Route: / (Protected)
         └─ ProtectedRoute ← Checks useAuth()
            └─ AppLayout
               ├─ AppSidebar
               │  ├─ Menu Items
               │  ├─ Logout Button
               │  └─ (calls supabase.auth.signOut)
               │
               ├─ Navbar
               │  ├─ Logo
               │  ├─ Language Switcher
               │  └─ User Dropdown
               │     └─ Logout Option
               │
               └─ Route Pages
                  ├─ Dashboard (displays user events)
                  ├─ CreateEvent
                  ├─ BrowseEvents
                  ├─ Registrations
                  ├─ Vendors
                  ├─ Venues
                  ├─ Calendar
                  ├─ Notifications
                  └─ Settings
```

---

## This Architecture Provides:

✅ **Scalability** - Vercel auto-scales, Supabase handles growth
✅ **Security** - Multiple layers of protection
✅ **Reliability** - 99.9% uptime SLA
✅ **Performance** - Global CDN, optimized queries
✅ **Maintainability** - Clear separation of concerns
✅ **Real-time** - WebSocket support (optional)
✅ **Offline Support** - Session persistence
✅ **Developer Experience** - Type-safe, hot reload
